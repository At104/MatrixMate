name: Build script for the branches (not main)

on:							
  push:
    branches:
      - devfunctions1/*
      - devfunctions2/*
      - testing/*

jobs:
  build:						
    runs-on: ubuntu-latest			
    steps:
      - uses: actions/checkout@v4		

      - name: build executable	
        run: make MatrixMate; ./MatrixMate; 	

      - name: Upload test results
        uses: actions/upload-artifact@v4	
        with:
          name: Test-Output		
          path: testOutput.txt	
